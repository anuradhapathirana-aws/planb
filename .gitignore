# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Never commit environment files (each sub-project also has its own rule)
.env
.env.*
!.env.example
# Placeholders only, no secrets — the server's starting point (backend/).
!.env.production.example

# Dependency directories (each sub-project also ignores its own)
node_modules/
vendor/
